syntax = "proto2";

package keras_classification.protos;

message Optimizer {
    enum Type {
        ADAM = 1;
        SGD = 2;
    }

    required Type type = 4;
    // initial learning rate
    required float init_lr = 5;
}
